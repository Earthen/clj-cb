(ns clj-cb.core-test
  (:require [clojure.test :refer :all]
            [clj-cb.core :refer :all]
            [earthen.clj-cb.bucket :as b]
            [earthen.clj-cb.cluster :as c]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
